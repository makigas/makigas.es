= render Makigas::LayoutComponent.new(position: :left) do |layout|
  - layout.with_sidebar do |sidebar|
    - sidebar.with_section(header: 'Search for something')
  This is the main portion of the site.
