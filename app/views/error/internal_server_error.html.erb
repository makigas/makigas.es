<header>
  <h1>HTTP 500</h1>
  <h2>😨 ¡Has roto makigas!</h2>
</header>

<p>
  Es broma. Puede que sea un problema temporal. O puede que sea un error de
  código en la programación de esta web. O puede que verdaderamente algo que
  hayas hecho haya causado un error en el servidor.
</p>

<p>
  Vuelve a intentarlo más adelante. Si continúas recibiendo este mensaje de
  error y consideras que no debería fallar nada, ponte en contacto para que
  podamos estar al tanto de esto.
</p>

<p>El ID de esta request fue: <%= request.uuid %></p>
