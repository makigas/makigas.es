<% content_for :hero do %>
  <h1>Insertar listas</h1>
<% end %>

<%= form_tag insert_topic_path(@topic), class: 'form-horizontal' do %>

<fieldset>
    <legend>Insertar lista de reproducción</legend>

    <div class="form-group row">
      <%= label_tag 'topic', 'Topic', class: 'col-sm-4 control-label' %>
      <div class="col-sm-8">
        <p class="form-control-static"><%= @topic.title %></p>
      </div>
    </div>

    <div class="form-group row">
      <%= label_tag 'playlist', 'Playlist', class: 'col-sm-4 control-label' %>
      <div class="col-sm-8">
      <%= select_tag 'playlist',
          options_from_collection_for_select(@playlists, :id, :title),
          { class: 'form-control' } %>
      </div>
    </div>

    <div class="row">
      <div class="col-sm-8 col-sm-offset-4">
        <%= button_tag 'Insertar', class: 'btn btn-success' %>
      </div>

<% end %>