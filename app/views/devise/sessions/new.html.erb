<div class="card-container">
  <div class="card text-black bg-light">
    <div class="card-header"><%= t('.log_in') %></div>
    <div class="card-body">
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <%= content_tag(:div, flash[:notice], class: 'alert alert-info') if flash[:notice] %>
        <%= content_tag(:div, flash[:alert], class: 'alert alert-danger') if flash[:alert] %>
        <div class="form-main">
          <%= f.input :email, required: false, autofocus: true %>
          <%= f.input :password, required: false %>
          <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
        </div>
        <%= f.button :submit, t('.log_in'), class: 'pull-right btn btn-primary' %>
      <% end %>
      <p class="internal">
        Este es un sistema de gestión de contenido interno y su uso no se permite por parte de personas no autorizadas.
        Si tienes una sugerencia o quieres reportar un error con el catálogo, por favor, envía un mensaje en su lugar.
      </p>
    </div>
  </div>
</div>
