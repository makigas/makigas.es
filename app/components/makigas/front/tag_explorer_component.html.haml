%ul.tagexplorer
  - tags.each do |tag|
    %li.tagexplorer__tag
      = link_to(link.call(tag), class: 'tagexplorer__link card') do
        = image_tag(url_for(tag.icon_variant(:default)), class: 'tagexplorer__logo', width: 64, alt: '')
        = tag.title
